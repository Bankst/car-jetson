# Disable logind-seat-gated BT monitor so A2DP sink works without an active
# user session (headless system service mode).
wireplumber.profiles = {
  main = {
    monitor.bluez.seat-monitoring = disabled
  }
}
